# 量盾（LiangDun）6.1 — 运行依赖
#
# 核心依赖
psutil>=5.9

# ── 可选增强 ──────────────────────────────────────────────
# 以下依赖缺失时程序会自动降级，不影响启动与基本功能。
# 如需完整功能，取消注释后安装。
#
# watchdog>=3.0        # 原生文件系统事件监控（缺失则回退到轮询扫描）
# pefile>=2023.2.7     # PE 文件解析，用于启发式评分（缺失则用简单启发式）
# pyminizip>=0.2.6     # 隔离区加密导出（缺失则导出未加密 ZIP）
# tkinterdnd2>=0.4.2   # 支持拖拽文件到窗口（缺失则只能用按钮选择）

# ── 打包为 EXE 时需要 ─────────────────────────────────────
# pyinstaller>=6.0

# ── 重要：ClamAV 引擎不是 Python 包 ───────────────────────
# 病毒扫描功能依赖 ClamAV 引擎（clamscan.exe / freshclam.exe）。
# 请从 https://www.clamav.net/downloads 下载 Windows x64 版（推荐 1.5.2），
# 解压后把整个 clamav 目录放到主程序同级。详见 README。
